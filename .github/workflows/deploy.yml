name: Deploy
on:
  push:
    branches: [main]

concurrency:
  group: deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - run: |
          cd ~/Portfolio
          git pull --ff-only
          docker compose up -d --build
          docker image prune -f