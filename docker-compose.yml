services:
  portfolio:
    build:
      context: .
      network: host
    container_name: portfolio-app
    restart: unless-stopped
    networks:
      - global-web

networks:
  global-web:
    external: true